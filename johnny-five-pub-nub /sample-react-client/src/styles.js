const pageStyles = {
    alignItems: "center",
    background: "#282c34",
    display: "flex",
    justifyContent: "center",
    minHeight: "100vh",
}

const footerStyles = {
    display: "flex",
}

const inputStyles = {
    flexGrow: 1,
    fontSize: "1.1rem",
    padding: "10px 15px",
}

const buttonStyles = {
    fontSize: "1.1rem",
    padding: "10px 15px",
}

export { pageStyles, footerStyles, inputStyles, buttonStyles }
